#include <stdio.h>

int main() {

    // operatii matematice in C

    // + (Adunare)
    // - (Scadere)
    // * (Inmultire)
    // / (Impartire)
    // % (Modulo - restul impartitii)
    // ++ (Incrementare)
    // -- (Decrementare)

    int x = 5;
    int y = 2;

    float z = x / (float) y;

    printf("%f", z);

    return 0;
}