#include <stdio.h>

int main() {
    
    // operatori augmentati in C sunt folositi pentru a modifica valoarea unei variabile cand se face o operatie pe ea insasi
    // x = x + 5;  ->  x += 5;

    int x = 10;

    // x += 2; // = 12
    // x -= 3; // = 7
    // x *= 4; // = 40
    // x /= 2; // = 5
    //x %= 2; // = 0

    printf("%d\n", x);
    
    return 0;

}