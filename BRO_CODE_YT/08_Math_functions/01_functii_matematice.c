#include <stdio.h>
#include <math.h>

int main()
{

    double A, B;
    int C, D, E;
    double F, G, H, I, J;

    A = sqrt(9);
    B = pow(2,4);
    C = round(3.14);
    D = ceil(3.14);
    E = floor(3.99);
    F = fabs(-3.1415);
    G = log(3);
    H = sin(45);
    I = cos(45);
    J = tan(45);

    printf("\n%lf", H);
    printf("\n%lf", I);
    printf("\n%lf", J);

    return 0;
}